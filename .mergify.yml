pull_request_rules:
  - name: Automatic merge for Dependabot pull requests
    conditions:
      - author=dependabot[bot]
      - status-success=CI / build (12.x) (pull_request)
    actions:
      merge:
        method: squash
